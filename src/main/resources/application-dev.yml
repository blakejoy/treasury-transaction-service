spring:
  datasource:
    url: jdbc:h2:mem:transactions
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    enabled: false
  h2:
    console:
      enabled: true
      path: /h2-console

logging:
  level:
    com.bjoynes.transactionsservice: DEBUG
